#include <stdio.h>
#include <stdlib.h>

int main()
{

    char S;
    char N[12];
    char P[12];
    int NT,A;
    printf("Nom:");
    scanf("%s",N);

    printf("Prenom:");
    scanf(" %s",P);

    printf("Age:");
    scanf(" %d",A);

    printf("Sexe:");
    scanf("%s",&S);

    printf("Nombre de telephone:");
    scanf(" %d",&NT);

return 0;
}
