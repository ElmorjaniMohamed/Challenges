#include <stdio.h>
#include <stdlib.h>

int main()
{
  int A;
  printf("Veuiller entrer la valeur de la nombre A:");
  scanf("%d",&A);
  if(A%2==0)

    printf("le nombre est pair");
  else
    printf("le nombre est impair ");

    return 0;
}
